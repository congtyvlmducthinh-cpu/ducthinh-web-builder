<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Kiểm Tra Giá - Vật Liệu Mới Đức Thịnh</title>
<script src="https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js"></script>
<style>:root {
  --primary: #1a56db;
  --primary-dark: #1e40af;
  --primary-glow: rgba(26,86,219,.2);
  --green: #059669;
  --green-light: #d1fae5;
  --amber: #d97706;
  --red: #dc2626;
  --bg: #f0f2f5;
  --card: #ffffff;
  --card-hover: #f8fafc;
  --text: #0f172a;
  --text-secondary: #475569;
  --muted: #94a3b8;
  --border: #e2e8f0;
  --radius-sm: 8px;
  --radius: 12px;
  --radius-lg: 16px;
  --shadow-sm: 0 1px 2px rgba(0,0,0,.04);
  --shadow: 0 1px 3px rgba(0,0,0,.06),0 1px 2px rgba(0,0,0,.04);
  --shadow-md: 0 4px 6px -1px rgba(0,0,0,.07),0 2px 4px -1px rgba(0,0,0,.04);
  --shadow-lg: 0 10px 25px -3px rgba(0,0,0,.08),0 4px 8px -2px rgba(0,0,0,.04);
  --font: 'Inter','SF Pro Display',-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,sans-serif;
}
*{margin:0;padding:0;box-sizing:border-box}
body{font-family:var(--font);background:var(--bg);color:var(--text);padding:20px;min-height:100vh;line-height:1.5}
.container{max-width:1440px;margin:0 auto}

/* HEADER */
header{
  background:linear-gradient(135deg,#0f1b3d 0%,#1a3a8a 40%,#1a56db 100%);
  color:#fff;
  border-radius:var(--radius-lg);
  padding:24px 28px;
  margin-bottom:20px;
  display:flex;
  flex-wrap:wrap;
  align-items:center;
  justify-content:space-between;
  gap:12px;
  box-shadow:0 4px 20px rgba(26,86,219,.25);
  position:relative;
  overflow:hidden;
}
header::before{
  content:'';
  position:absolute;
  top:-50%;
  right:-20%;
  width:300px;
  height:300px;
  background:radial-gradient(circle,rgba(255,255,255,.06) 0%,transparent 70%);
  pointer-events:none;
}
header::after{
  content:'';
  position:absolute;
  bottom:-30%;
  left:10%;
  width:200px;
  height:200px;
  background:radial-gradient(circle,rgba(255,255,255,.04) 0%,transparent 70%);
  pointer-events:none;
}
header h1{
  font-size:22px;
  font-weight:800;
  letter-spacing:-.3px;
  position:relative;
  z-index:1;
}
header h1 span{color:#93bbff}
header .sub{
  font-size:13px;
  opacity:.8;
  font-weight:500;
  margin-top:2px;
  position:relative;
  z-index:1;
}
header .badge{
  background:rgba(255,255,255,.12);
  backdrop-filter:blur(8px);
  padding:6px 16px;
  border-radius:20px;
  font-size:12px;
  font-weight:600;
  letter-spacing:.02em;
  position:relative;
  z-index:1;
  border:1px solid rgba(255,255,255,.1);
}
header .actions{
  display:flex;
  gap:8px;
  flex-wrap:wrap;
  position:relative;
  z-index:1;
}

/* BUTTONS */
.btn,.dl-link,.upload-btn{
  display:inline-flex;
  align-items:center;
  gap:5px;
  padding:7px 16px;
  border-radius:9px;
  font-size:12px;
  font-weight:600;
  cursor:pointer;
  border:none;
  font-family:inherit;
  transition:all .2s;
  white-space:nowrap;
  text-decoration:none;
}
.btn-primary,.dl-link{
  background:rgba(255,255,255,.15);
  color:#fff;
  backdrop-filter:blur(4px);
  border:1px solid rgba(255,255,255,.12);
}
.btn-primary:hover,.dl-link:hover{
  background:rgba(255,255,255,.25);
  transform:translateY(-1px);
}
.btn-primary:active,.dl-link:active{transform:translateY(0)}
.btn-outline{
  background:transparent;
  color:var(--primary);
  border:1.5px solid var(--primary);
}
.btn-outline:hover{background:var(--primary-glow)}
.btn-soft{
  background:#f1f5f9;
  color:var(--text);
  border:1px solid var(--border);
}
.btn-soft:hover{background:#e2e8f0}
.btn-success{
  background:var(--green);
  color:#fff;
}
.btn-success:hover{background:#047857}

/* TABS */
.tabs{
  display:flex;
  gap:3px;
  background:var(--card);
  border-radius:var(--radius);
  padding:4px;
  margin-bottom:20px;
  box-shadow:var(--shadow);
  overflow-x:auto;
  scrollbar-width:none;
  position:sticky;
  top:12px;
  z-index:100;
  border:1px solid var(--border);
}
.tab-btn{
  flex-shrink:0;
  padding:10px 18px;
  border:none;
  border-radius:9px;
  background:transparent;
  color:var(--text-secondary);
  font-size:13px;
  font-weight:600;
  cursor:pointer;
  transition:all .25s;
  white-space:nowrap;
}
.tab-btn:hover{background:#f1f5f9;color:var(--text)}
.tab-btn.active{
  background:linear-gradient(135deg,#1a56db,#2563eb);
  color:#fff;
  box-shadow:0 2px 8px rgba(26,86,219,.3);
}

/* CONTROLS */
.controls{
  display:flex;
  flex-wrap:wrap;
  gap:10px;
  margin-bottom:18px;
  align-items:center;
}
.controls input,.controls select{
  padding:10px 14px;
  border:1.5px solid var(--border);
  border-radius:10px;
  font-size:14px;
  background:var(--card);
  outline:none;
  min-width:160px;
  flex:1;
  transition:all .2s;
  color:var(--text);
  font-family:var(--font);
}
.controls input:focus,.controls select:focus{
  border-color:var(--primary);
  box-shadow:0 0 0 3px var(--primary-glow);
}
.currency-toggle{
  display:flex;
  background:var(--card);
  border:1.5px solid var(--border);
  border-radius:10px;
  overflow:hidden;
  flex-shrink:0;
}
.currency-toggle button{
  padding:10px 16px;
  border:none;
  background:transparent;
  font-weight:700;
  font-size:13px;
  cursor:pointer;
  transition:all .2s;
  color:var(--text-secondary);
  font-family:var(--font);
}
.currency-toggle button.active{
  background:linear-gradient(135deg,#1a56db,#2563eb);
  color:#fff;
}

/* SUMMARY CARDS */
.summary-bar{
  display:flex;
  flex-wrap:wrap;
  gap:12px;
  margin-bottom:18px;
}
.summary-card{
  background:var(--card);
  border-radius:var(--radius);
  padding:16px 20px;
  flex:1;
  min-width:130px;
  box-shadow:var(--shadow-sm);
  border:1px solid var(--border);
  transition:all .2s;
}
.summary-card:hover{
  box-shadow:var(--shadow-md);
  border-color:#cbd5e1;
}
.summary-card .lbl{
  font-size:12px;
  color:var(--muted);
  font-weight:600;
  text-transform:uppercase;
  letter-spacing:.04em;
  margin-bottom:4px;
}
.summary-card .val{
  font-size:24px;
  font-weight:800;
  color:var(--primary);
  letter-spacing:-.5px;
}

/* TABLE */
.table-wrap{
  background:var(--card);
  border-radius:var(--radius-lg);
  overflow:hidden;
  box-shadow:var(--shadow);
  border:1px solid var(--border);
}
table{
  width:100%;
  border-collapse:collapse;
  font-size:13px;
  min-width:640px;
}
thead{position:sticky;top:0;z-index:2}
thead tr:first-child th{
  background:linear-gradient(135deg,#f8fafc,#f1f5f9);
  padding:8px 12px;
  font-weight:600;
  font-size:11px;
  text-transform:uppercase;
  letter-spacing:.04em;
  color:var(--muted);
  border-bottom:2px solid var(--border);
}
thead tr:nth-child(2) th{
  background:#f8fafc;
  padding:10px 12px;
  font-weight:700;
  font-size:12px;
  color:var(--text);
  border-bottom:1px solid var(--border);
  white-space:nowrap;
}
td{
  padding:10px 12px;
  border-bottom:1px solid #f1f5f9;
  white-space:nowrap;
  transition:background .15s;
}
tbody tr{transition:background .15s}
tbody tr:nth-child(even) td{background:#fafbfc}
tbody tr:hover td{background:#eff6ff}
.text-right{text-align:right}
.info-row{font-size:11px;color:var(--muted);font-weight:400;text-transform:none;letter-spacing:0}

/* BADGES */
.badge-spec{
  display:inline-block;
  padding:3px 10px;
  border-radius:20px;
  font-size:11px;
  font-weight:600;
  letter-spacing:.02em;
}
.badge-A1{background:#dbeafe;color:#1e40af}
.badge-A2{background:#dcfce7;color:#15803d}
.badge-A2-{background:#fef9c3;color:#a16207}
.badge-A3{background:#fce7f3;color:#be185d}
.badge-Jumbo{background:#fef3c7;color:#92400e}
.badge-25KG{background:#e0f2fe;color:#0369a1}
.badge-other{background:#f3e8ff;color:#6b21a8}
.tag-profit{
  display:inline-block;
  padding:2px 8px;
  border-radius:20px;
  background:var(--green-light);
  color:#166534;
  font-size:11px;
  font-weight:700;
}


/* CALC */
.calc-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:20px;margin:16px 0}.calc-block{background:var(--card);border-radius:var(--radius);padding:20px;box-shadow:var(--shadow)}.calc-block h3{font-size:14px;font-weight:700;color:var(--primary);margin-bottom:14px;display:flex;align-items:center;gap:6px}.calc-row{display:flex;flex-direction:column;gap:4px;margin-bottom:12px}.calc-row label{font-size:12px;font-weight:600;color:var(--text-secondary)}.calc-row select{padding:8px 10px;border:1px solid var(--border);border-radius:var(--radius-sm);font-size:13px;background:#fff}.calc-result-block{grid-column:1/-1}.calc-empty{padding:24px;text-align:center;color:var(--muted);font-size:13px}.calc-result-item{display:flex;justify-content:space-between;padding:6px 0;border-bottom:1px solid var(--border);font-size:13px}.calc-result-item span{color:var(--text-secondary)}.calc-total{font-weight:700;font-size:15px!important;color:var(--primary);border-bottom:none!important;padding:10px 0!important}.calc-commission-wrap{border-top:2px dashed var(--border);margin-top:12px;padding-top:12px}.calc-comm-row{display:flex;justify-content:space-between;padding:5px 0;font-size:13px}.calc-comm-row span{color:var(--text-secondary)}
.calc-grid{
  display:grid;
  grid-template-columns:1fr 1fr;
  gap:24px;
  padding:24px;
  background:#f8fafc;
}
.calc-left,.calc-right{
  background:var(--card);
  border-radius:var(--radius);
  padding:24px;
  box-shadow:var(--shadow-sm);
  border:1px solid var(--border);
}
.calc-left h3,.calc-right h3{
  font-size:15px;
  margin-bottom:16px;
  padding-bottom:10px;
  border-bottom:2px solid var(--primary);
  color:var(--primary);
  font-weight:700;
  display:flex;
  align-items:center;
  gap:6px;
}
.calc-row{margin-bottom:16px}
.calc-row:last-child{margin-bottom:0}
.calc-row label{
  display:block;
  font-size:12px;
  font-weight:700;
  margin-bottom:5px;
  color:var(--text-secondary);
  text-transform:uppercase;
  letter-spacing:.03em;
}
.calc-row select,.calc-row input{
  width:100%;
  padding:11px 14px;
  border:1.5px solid var(--border);
  border-radius:10px;
  font-size:14px;
  background:var(--card);
  outline:none;
  transition:all .2s;
  font-family:var(--font);
  color:var(--text);
}
.calc-row select:focus,.calc-row input:focus{
  border-color:var(--primary);
  box-shadow:0 0 0 3px var(--primary-glow);
}
.calc-result{
  background:linear-gradient(135deg,#f0fdf4,#ecfdf5);
  border:1.5px solid #bbf7d0;
  border-radius:var(--radius);
  padding:20px;
  margin-top:0;
}
.calc-result h4{
  font-size:14px;
  color:var(--green);
  margin-bottom:14px;
  font-weight:700;
  display:flex;
  align-items:center;
  gap:6px;
}
.calc-result .line{
  display:flex;
  justify-content:space-between;
  align-items:center;
  padding:8px 0;
  font-size:14px;
  border-bottom:1px dashed #d1fae5;
}
.calc-result .line:last-child{
  border-bottom:none;
  font-weight:800;
  font-size:17px;
  padding-top:12px;
  color:var(--green);
}
.calc-result .line .lbl{font-weight:500;color:var(--text-secondary)}
.calc-result .line .val{font-weight:700}
.calc-empty{
  padding:40px 20px;
  text-align:center;
  color:var(--muted);
  font-size:14px;
  line-height:1.7;
}
.calc-hint{
  margin-top:16px;
  padding:14px 18px;
  background:linear-gradient(135deg,#f1f5f9,#f8fafc);
  border-radius:var(--radius);
  font-size:12px;
  color:var(--text-secondary);
  border:1px solid var(--border);
}
.calc-hint strong{color:var(--text)}

/* UPLOAD PANEL */
.upload-panel{
  background:var(--card);
  border-radius:var(--radius-lg);
  padding:24px;
  margin-top:-6px;
  margin-bottom:18px;
  border:2px dashed #cbd5e1;
  display:none;
  box-shadow:var(--shadow-sm);
}
.upload-panel.open{display:block;animation:slideDown .3s ease}
@keyframes slideDown{from{opacity:0;transform:translateY(-10px)}to{opacity:1;transform:translateY(0)}}
.upload-panel h3{
  font-size:15px;
  margin-bottom:14px;
  color:var(--primary);
  font-weight:700;
  display:flex;
  align-items:center;
  gap:6px;
}
.upload-area{
  border:2px dashed var(--border);
  border-radius:var(--radius-lg);
  padding:36px 20px;
  text-align:center;
  cursor:pointer;
  transition:all .25s;
  color:var(--muted);
  background:#fafbfc;
}
.upload-area:hover,.upload-area.dragover{
  border-color:var(--primary);
  background:var(--primary-glow);
  color:var(--primary);
  transform:scale(1.01);
}
.upload-status{
  margin-top:12px;
  padding:10px 16px;
  border-radius:9px;
  font-size:13px;
  font-weight:600;
  display:none;
}
.upload-status.ok{background:#dcfce7;color:#166534;display:block}
.upload-status.err{background:#fee2e2;color:#991b1b;display:block}

/* MANAGE PANEL */
.manage-panel{display:none;animation:fadeIn .3s ease}
.manage-panel.open{display:block}
@keyframes fadeIn{from{opacity:0}to{opacity:1}}
.manage-login{
  max-width:400px;
  margin:40px auto;
  background:var(--card);
  border-radius:var(--radius-lg);
  padding:32px;
  text-align:center;
  box-shadow:var(--shadow-md);
  border:1px solid var(--border);
}
.manage-login h3{
  font-size:20px;
  margin-bottom:6px;
  color:var(--text);
}
.manage-login p{
  font-size:14px;
  color:var(--muted);
  margin-bottom:20px;
}
.manage-login input{
  width:100%;
  padding:12px 16px;
  border:1.5px solid var(--border);
  border-radius:10px;
  font-size:15px;
  outline:none;
  transition:all .2s;
  font-family:var(--font);
  margin-bottom:12px;
  text-align:center;
}
.manage-login input:focus{
  border-color:var(--primary);
  box-shadow:0 0 0 3px var(--primary-glow);
}
.manage-login button{
  width:100%;
  padding:12px;
  background:linear-gradient(135deg,#1a56db,#2563eb);
  color:#fff;
  border:none;
  border-radius:10px;
  font-size:15px;
  font-weight:700;
  cursor:pointer;
  transition:all .2s;
  font-family:var(--font);
}
.manage-login button:hover{transform:translateY(-1px);box-shadow:0 4px 12px rgba(26,86,219,.3)}
.manage-error{
  margin-top:10px;
  padding:8px;
  border-radius:8px;
  background:#fee2e2;
  color:#991b1b;
  font-size:13px;
  font-weight:600;
  display:none;
}
.manage-dashboard{display:none;animation:fadeIn .3s ease}
.manage-dashboard.open{display:block}
.manage-grid{
  display:grid;
  grid-template-columns:repeat(auto-fill,minmax(180px,1fr));
  gap:14px;
  margin-bottom:24px;
}
.manage-card{
  background:var(--card);
  border-radius:var(--radius);
  padding:20px;
  text-align:center;
  box-shadow:var(--shadow-sm);
  border:1px solid var(--border);
  transition:all .2s;
}
.manage-card:hover{box-shadow:var(--shadow-md);border-color:#cbd5e1;transform:translateY(-2px)}
.manage-card .num{
  font-size:28px;
  font-weight:800;
  color:var(--primary);
  margin-bottom:4px;
  letter-spacing:-1px;
}
.manage-card .lbl{
  font-size:12px;
  color:var(--muted);
  font-weight:600;
  text-transform:uppercase;
  letter-spacing:.04em;
}
.manage-actions{
  background:var(--card);
  border-radius:var(--radius-lg);
  padding:24px;
  margin-bottom:16px;
  box-shadow:var(--shadow-sm);
  border:1px solid var(--border);
}
.manage-actions h3{
  font-size:15px;
  font-weight:700;
  margin-bottom:14px;
  color:var(--text);
  display:flex;
  align-items:center;
  gap:6px;
}
.manage-actions .btn-row{
  display:flex;
  gap:10px;
  flex-wrap:wrap;
  margin-top:12px;
}
.manage-actions button{
  display:inline-flex;
  align-items:center;
  gap:5px;
  padding:10px 20px;
  border-radius:10px;
  border:none;
  font-size:13px;
  font-weight:600;
  cursor:pointer;
  transition:all .2s;
  font-family:var(--font);
}
.manage-actions button.primary{
  background:linear-gradient(135deg,#1a56db,#2563eb);
  color:#fff;
  box-shadow:0 2px 8px rgba(26,86,219,.2);
}
.manage-actions button.primary:hover{
  transform:translateY(-1px);
  box-shadow:0 4px 14px rgba(26,86,219,.3);
}
.manage-actions button:not(.primary){
  background:#f1f5f9;
  color:var(--text);
  border:1px solid var(--border);
}
.manage-actions button:not(.primary):hover{background:#e2e8f0}
.upload-sm{
  border:2px dashed var(--border);
  border-radius:var(--radius);
  padding:24px;
  text-align:center;
  cursor:pointer;
  transition:all .25s;
  color:var(--muted);
  background:#fafbfc;
}
.upload-sm:hover,.upload-sm.dragover{
  border-color:var(--primary);
  background:var(--primary-glow);
  color:var(--primary);
}
.manage-status-sm{
  margin-top:10px;
  padding:8px 14px;
  border-radius:8px;
  font-size:12px;
  font-weight:600;
  display:none;
}
.manage-status-sm.ok{background:#dcfce7;color:#166534;display:block}
.manage-status-sm.err{background:#fee2e2;color:#991b1b;display:block}

/* MODAL */
.modal-overlay{
  position:fixed;
  top:0;left:0;width:100%;height:100%;
  background:rgba(15,23,42,.6);
  backdrop-filter:blur(4px);
  display:none;
  justify-content:center;
  align-items:center;
  z-index:2000;
}
.modal-overlay.open{display:flex}
.modal-box{
  background:var(--card);
  border-radius:var(--radius-lg);
  padding:36px;
  max-width:420px;
  width:90%;
  box-shadow:0 20px 60px rgba(0,0,0,.15);
  animation:modalIn .25s ease;
}
@keyframes modalIn{
  from{opacity:0;transform:scale(.95) translateY(10px)}
  to{opacity:1;transform:scale(1) translateY(0)}
}
.modal-box h3{
  font-size:20px;
  margin-bottom:6px;
  color:var(--text);
  font-weight:800;
}
.modal-box p{
  font-size:14px;
  color:var(--muted);
  margin-bottom:20px;
}
.modal-box input{
  width:100%;
  padding:12px 16px;
  border:2px solid var(--border);
  border-radius:10px;
  font-size:16px;
  outline:none;
  transition:all .2s;
  font-family:var(--font);
  color:var(--text);
}
.modal-box input:focus{
  border-color:var(--primary);
  box-shadow:0 0 0 3px var(--primary-glow);
}
.pw-error{
  background:#fee2e2;
  color:#991b1b;
  padding:10px 14px;
  border-radius:8px;
  font-size:13px;
  font-weight:600;
  margin-top:12px;
  display:none;
}
.btn-row{
  display:flex;
  gap:10px;
  margin-top:18px;
  justify-content:flex-end;
}
.btn-cancel,.btn-confirm{
  padding:11px 28px;
  border-radius:10px;
  border:none;
  font-size:14px;
  font-weight:700;
  cursor:pointer;
  transition:all .2s;
  font-family:var(--font);
}
.btn-cancel{
  background:#f1f5f9;
  color:var(--text-secondary);
}
.btn-cancel:hover{background:#e2e8f0}
.btn-confirm{
  background:linear-gradient(135deg,#1a56db,#2563eb);
  color:#fff;
  box-shadow:0 2px 8px rgba(26,86,219,.2);
}
.btn-confirm:hover{
  transform:translateY(-1px);
  box-shadow:0 4px 14px rgba(26,86,219,.3);
}


/* ANIMATIONS */
@keyframes pulse{0%,100%{opacity:1}50%{opacity:.5}}
@keyframes shimmer{0%{background-position:-200% 0}100%{background-position:200% 0}}
@keyframes slideIn{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:translateY(0)}}
@keyframes scaleIn{from{opacity:0;transform:scale(.96)}to{opacity:1;transform:scale(1)}}
@keyframes float{0%,100%{transform:translateY(0)}50%{transform:translateY(-4px)}}
.loading{opacity:.5;transition:opacity .2s}
.loading-done{opacity:1;transition:opacity .2s}
#mainContainer{transition:opacity .2s ease;animation:slideIn .3s ease}
tbody tr{animation:slideIn .2s ease both}
tbody tr:nth-child(1){animation-delay:.02s}
tbody tr:nth-child(2){animation-delay:.04s}
tbody tr:nth-child(3){animation-delay:.06s}
tbody tr:nth-child(4){animation-delay:.08s}
tbody tr:nth-child(5){animation-delay:.1s}
tbody tr:nth-child(6){animation-delay:.12s}
tbody tr:nth-child(7){animation-delay:.14s}
tbody tr:nth-child(8){animation-delay:.16s}
.summary-card{animation:scaleIn .3s ease both}
.summary-card:nth-child(1){animation-delay:.05s}
.summary-card:nth-child(2){animation-delay:.1s}
.summary-card:nth-child(3){animation-delay:.15s}
.summary-card:nth-child(4){animation-delay:.2s}

/* SCROLLBAR */
::-webkit-scrollbar{width:6px;height:6px}
::-webkit-scrollbar-track{background:transparent}
::-webkit-scrollbar-thumb{background:#cbd5e1;border-radius:3px}
::-webkit-scrollbar-thumb:hover{background:#94a3b8}

/* SELECTION */
::selection{background:var(--primary);color:#fff}

/* FOCUS RING */
*:focus-visible{outline:2px solid var(--primary);outline-offset:2px}

/* TABLE STICKY COL */
td:first-child{position:sticky;left:0;background:inherit;z-index:1}
tbody tr:hover td:first-child{background:#eff6ff}
/* TABLET / MOBILE */
@media(max-width:900px){
  .calc-grid{grid-template-columns:1fr;gap:16px}
}
@media(max-width:768px){
  body{padding:12px}
  header{padding:18px 20px}
  header h1{font-size:18px}
  .controls input,.controls select{min-width:100px}
  table{font-size:12px}
  th,td{padding:7px 6px}
  .summary-card{min-width:100px}
  .summary-card .val{font-size:20px}
  .manage-grid{grid-template-columns:repeat(2,1fr)}
}
@media(max-width:480px){
  body{padding:8px}
  header{padding:14px 16px}
  header h1{font-size:16px}
  header .sub{font-size:11px}
  .tab-btn{padding:8px 12px;font-size:12px}
  .manage-grid{grid-template-columns:1fr 1fr}
  .manage-login{margin:20px auto;padding:24px}
  .modal-box{padding:24px}
}
/* PRICE MODE */
.price-mode-bar{display:flex;flex-wrap:wrap;align-items:center;gap:10px;padding:12px 18px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius);margin-bottom:14px}
.price-mode-bar .mode-group{display:flex;gap:4px}
.price-mode-bar .mode-btn{padding:7px 18px;border:1.5px solid var(--border);background:#fff;font-size:13px;font-weight:600;cursor:pointer;border-radius:8px;transition:all .2s;font-family:var(--font);color:var(--text-secondary)}
.price-mode-bar .mode-btn.active{background:linear-gradient(135deg,#1a56db,#2563eb);color:#fff;border-color:var(--primary);box-shadow:0 2px 8px rgba(26,86,219,.2)}
.price-mode-bar .lcc-group{display:flex;gap:4px;align-items:center}
.price-mode-bar .lcc-btn{padding:5px 14px;border:1.5px solid var(--border);background:#fff;font-size:12px;font-weight:600;cursor:pointer;border-radius:6px;transition:all .2s;font-family:var(--font);color:var(--text-secondary)}
.price-mode-bar .lcc-btn.active{background:linear-gradient(135deg,#059669,#10b981);color:#fff;border-color:#059669}
.price-mode-bar .ext-input{padding:6px 12px;border:1.5px solid var(--border);border-radius:6px;font-size:12px;font-weight:600;width:100px;outline:none;font-family:var(--font);color:var(--text);text-align:right}
.price-mode-bar .ext-input:focus{border-color:var(--primary);box-shadow:0 0 0 3px var(--primary-glow)}
.price-mode-bar .ext-label{font-size:11px;font-weight:600;color:var(--muted);white-space:nowrap}
@media(max-width:768px){.price-mode-bar{gap:8px;padding:10px 12px;flex-direction:column;align-items:stretch}.price-mode-bar .mode-group{width:100%}.price-mode-bar .mode-btn{flex:1;text-align:center;font-size:12px;padding:6px 10px}.price-mode-bar .lcc-group{width:100%;justify-content:center}.price-mode-bar .ext-input{width:80px}}


.freight-warning{background:#fef3cd;border:1px solid #fbbf24;color:#92400e;padding:12px 16px;border-radius:10px;margin-bottom:16px;font-size:14px;font-weight:500;display:flex;align-items:center;gap:8px}

.calc-price-mode-bar{display:flex;flex-wrap:wrap;align-items:center;gap:10px;margin-bottom:16px;padding:12px 16px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)}
.calc-price-mode-bar .mode-group{display:flex;gap:6px}
.calc-price-mode-bar .mode-btn{padding:7px 16px;border:1px solid var(--border);background:var(--card);color:var(--text-secondary);border-radius:8px;cursor:pointer;font-size:13px;font-weight:500;transition:all .2s}
.calc-price-mode-bar .mode-btn:hover{border-color:var(--primary);color:var(--primary)}
.calc-price-mode-bar .mode-btn.active{background:var(--primary);color:#fff;border-color:var(--primary);box-shadow:0 2px 8px var(--primary-glow)}
#calcLccGroup,#calcFreightGroup{display:inline-flex;align-items:center;gap:6px;font-size:13px;color:var(--text-secondary)}

</style>
</head>
<body>
<div class="container">
<header>
<div>
<h1 data-i18n="header.title">Kiểm Tra Giá <span>🔍</span></h1>
<div style="display:flex;gap:4px;margin-top:6px"><button class="lang-btn active" data-lang="vi" onclick="setLang('vi')">🇻🇳 VI</button><button class="lang-btn" data-lang="en" onclick="setLang('en')">🇬🇧 EN</button><button class="lang-btn" data-lang="zh" onclick="setLang('zh')">🇨🇳 CN</button></div>
<div class="sub" data-i18n="header.sub" style="margin-top:6px;font-size:13px;opacity:.8;">Vật Liệu Mới Đức Thịnh - Bảng giá tháng 06/2026</div>
</div>
<div style="display:flex;gap:8px;align-items:center;position:relative;z-index:1;flex-wrap:wrap"><span class="badge" data-i18n="header.badge">🏭 VL MỚI ĐỨC THỊNH</span><span class="badge" id="dataInfo">📊 0 SP · 0 BB · 0 QC</span></div>
</header>

<div class="upload-status" id="uploadStatus"></div>
</div>
<div class="tabs" id="tabs">
<button class="tab-btn active" data-tab="pricelist" onclick="switchTab('pricelist')"><span>📋 Giá bán</span><br><small style="font-weight:400;font-size:10px;opacity:.7">Bảng giá</small></button>
<button class="tab-btn" data-tab="calc" onclick="switchTab('calc')"><span>📝 Tính giá</span><br><small style="font-weight:400;font-size:10px;opacity:.7">Tính giá thành</small></button>
<button class="tab-btn" data-tab="manage" onclick="switchTab('manage')"><span>⚙️ Quản lý</span><br><small style="font-weight:400;font-size:10px;opacity:.7">Upload dữ liệu</small></button>
</div>
<div class="controls" id="controlBar">
<input type="text" id="searchInput" placeholder="🔍 Tìm sản phẩm (mã, kích thước...)" oninput="globalSearch()">
<select id="specFilter" onchange="render()"><option value="">Tất cả tiêu chuẩn</option><option value="A1">A1</option><option value="A2">A2</option><option value="A2-">A2-</option><option value="A3">A3</option></select>
<select id="sizeFilter" onchange="render()"><option value="">Tất cả kích thước</option></select>
<select id="machineFilter" onchange="render()"><option value="">Tất cả máy</option></select>
<div class="currency-toggle">
<button class="active" data-currency="VND" onclick="toggleCurrency(this)">VND</button>
<button data-currency="USD" onclick="toggleCurrency(this)">USD</button>
</div>
</div>
</div>



<div class="price-mode-bar" id="priceModeBar" style="display:none">
  <div class="mode-group">
    <button class="mode-btn active" data-mode="exw" onclick="setPriceMode('exw')">EXW</button>
    <button class="mode-btn" data-mode="fob" onclick="setPriceMode('fob')">FOB</button>
    <button class="mode-btn" data-mode="cif" onclick="setPriceMode('cif')">CIF</button>
  </div>
  <div class="lcc-group" id="lccGroup" style="display:none">
    <span class="ext-label">Lo&#7841;i LCC:</span>
    <button class="lcc-btn active" data-lcc="no" onclick="setLccType('no')">No Lcc</button>
    <button class="lcc-btn" data-lcc="sub" onclick="setLccType('sub')">Sub Lcc</button>
  </div>
  <div id="freightGroup" style="display:none">
    <span class="ext-label">&#128674; C&#432;&#7899;c bi&#7875;n:</span>
    <input type="number" class="ext-input" id="freightInput" value="0" min="0" step="100" oninput="onFreightChange()" style="width:100px">
    <span class="ext-label">USD</span>
  </div>
</div>
<div id="mainContainer"></div>
<!-- Manage -->
<div class="manage-panel" id="managePanel">
  <div class="manage-login" id="manageLogin">
    <h3>⚙️ Quản lý dữ liệu</h3>
    <p>🔒 Nhập mật khẩu để tải / upload / cập nhật dữ liệu</p>
    <input type="password" id="managePass" placeholder="🔑 Nhập mật khẩu..." onkeydown="if(event.key==='Enter')manageLogin()" autocomplete="off">
    <button onclick="manageLogin()">🔑 Đăng nhập</button>
    <div class="manage-error" id="manageError"></div>
  </div>
  <div class="manage-dashboard" id="manageDashboard">
    <div class="manage-grid">
      <div class="manage-card"><div class="num" id="mProdCnt">0</div><div class="lbl">Sản phẩm</div></div>
      <div class="manage-card"><div class="num" id="mBagCnt">0</div><div class="lbl">Bao bì</div></div>
      <div class="manage-card"><div class="num" id="mOtherCnt">0</div><div class="lbl">Quy cách khác</div></div>
      <div class="manage-card"><div class="num" id="mUptd">06/2026</div><div class="lbl">Cập nhật</div></div>
    </div>
    <div class="manage-actions">
      <h3>⬇ Tải dữ liệu hiện tại</h3><div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(190px,1fr));gap:8px;margin-bottom:10px"><button class="primary" onclick="downloadFile(0)">📋 Giá bán tối thiểu</button><button class="primary" onclick="downloadFile(1)">📦 Surcharge bao bì</button><button class="primary" onclick="downloadFile(2)">📐 Quy cách khác</button></div>
      <div class="btn-row">
        <button class="primary" onclick="downloadAsExcel()">📊 Tải Excel (.xlsx)</button>
        <button onclick="downloadAsJSON()">📋 Tải JS</button>
      </div>
    </div>
    <div class="manage-actions">
      <h3>📤 Upload dữ liệu mới</h3>
      <p style="font-size:0.85rem;color:var(--muted);margin-bottom:8px;">Upload file Excel (.xlsx) để cập nhật</p>
      <div class="upload-sm" id="manageDropZone"><div style="font-size:1.5rem;margin-bottom:6px">📂</div><div><strong>Kéo thả file vào đây</strong><br>hoặc bấm để chọn</div></div>
      <input type="file" id="manageFileInput" accept=".xlsx,.xls" style="display:none">
      <div class="manage-status-sm" id="manageUploadStatus"></div>
    </div>
  </div>
</div>
</div>


<div class="modal-overlay" id="pwModal">
<div class="modal-box">
<h3>🔐 Nhập mật khẩu</h3>
<p>🔒 Chức năng quản lý dữ liệu yêu cầu mật khẩu</p>
<input type="password" id="pwInput" placeholder="🔑 Nhập mật khẩu..." onkeydown="if(event.key==='Enter')checkPassword()" autocomplete="off">
<div class="pw-error" id="pwError">❌ Sai mật khẩu, thử lại!</div>
<div class="btn-row">
<button class="btn-cancel" onclick="closePwModal()">Hủy</button>
<button class="btn-confirm" onclick="checkPassword()">Xác nhận</button>
</div>
</div>
</div>

</body></html>