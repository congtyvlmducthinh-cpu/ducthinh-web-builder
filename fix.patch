*** Begin Patch ***
--- a/sites/kiem-tra-gia/index.html
+++ b/sites/kiem-tra-gia/index.html
@@ -198,7 +198,7 @@
 .btn-sm:hover{background:var(--hover-bg,#f0f4ff);border-color:var(--primary,#1a56db);color:var(--primary,#1a56db)}
 
-/* Max loading toggle */ .ml-toggle { display: none; }
+/* Max loading toggle */ .ml-toggle { display: none; } .ml-toggle.ml-visible { display: table-cell; }
 </style>
 </head>
 <body>
@@ -1489,12 +1489,14 @@
 function toggleMaxLoad() {
   var cols = document.querySelectorAll('.ml-toggle');
   var btn = document.getElementById('mlToggleBtn');
-  var hidden = btn.textContent.indexOf('Hiện') >= 0;
-  cols.forEach(function(el) { el.style.display = hidden ? '' : 'none'; });
+  var hidden = btn.textContent.indexOf('Hiện') >= 0;
+  cols.forEach(function(el) { el.classList.toggle('ml-visible', hidden); });
   btn.textContent = hidden ? '📋 Ẩn max tải' : '📋 Hiện max tải';
 }
*** End Patch ***